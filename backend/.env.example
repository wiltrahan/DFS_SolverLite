# PostgreSQL container settings (docker compose)
POSTGRES_DB=dfs_solverlite
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-password

# Backend datasource settings (Spring Boot)
DB_URL=jdbc:postgresql://localhost:5432/dfs_solverlite
DB_USERNAME=postgres
DB_PASSWORD=change-this-password

# CORS (frontend origin)
APP_CORS_ORIGIN=http://localhost:3000,http://localhost:5173

